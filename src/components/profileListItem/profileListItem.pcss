.info-list > div {
    display: flex;
    justify-content: space-between;
    border-bottom: 1px solid var(--color-light);
    padding: 4px 0;
    margin-bottom: 8px;

    & .info-list_header {
        font-weight: bold;
    }

    & .info-list_value {
        color: var(--color-blue-light);
    }
}